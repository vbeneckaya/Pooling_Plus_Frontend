namespace Domain.Enums
{
    public enum FieldType
    {
        Text,
        BigText,
        State,
        Number,
        Enum,
        Select,
        Boolean,
        DateTime,
        Date,
        Time,
        Link,
        LocalDateTime,
        Password
    }
}