namespace Domain.Enums
{
    public enum FieldType
    {
        Text,
        State,
        Number,
        Enum,
        Select,
        Boolean,
        DateTime,
        Date,
        Time,
        Link,
    }
}