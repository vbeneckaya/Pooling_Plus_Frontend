namespace Domain.Enums
{
    public enum FiledType
    {
        Text,
        State,
        Number,
        Enum,
        Select,
        Boolean,
        DateTime,
        Date,
        Time,
    }
}