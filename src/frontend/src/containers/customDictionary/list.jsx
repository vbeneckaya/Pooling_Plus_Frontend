import React, { Component } from 'react';
import PropTypes from 'prop-types';
import SuperGrid from "../../components/SuperGrid";

const List = () => {
    return <SuperGrid columns={[]}/>;
};
List.propTypes = {};

export default List;
